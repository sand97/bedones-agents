# Base URL of the Bedones backend API (shares the session cookie).
# The studio is served from design.bedones.com and talks to
# api-moderator.bedones.com with credentials: 'include'.
VITE_API_URL=https://api-moderator.bedones.com
